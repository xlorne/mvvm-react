import React  from "react";
import {ProTable} from "@ant-design/pro-components";

const User = () => {
    return (
        <ProTable
            dataSource={[]}
            columns={[]}
            rowKey="id"
            headerTitle="User List"
        />
    );
}

export default User;
